apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-head-right-arm
  namespace: {{ default "podtato" .Release.Name }}-helm
  labels:
    {{- include "head" . | indent 2 }}
spec:
  selector:
    matchLabels:
      {{- include "headRightArm" . | indent 4 }}
  template:
    metadata:
      labels:
        {{- include "headRightArm" . | indent 6 }}
    spec:
      terminationGracePeriodSeconds: 5
      containers:
      - name: {{ .Values.rightArm.image.name }}
        image:  {{ .Values.rightArm.image.repository }}:{{ .Values.rightArm.image.tag }}
        imagePullPolicy: {{ .Values.rightArm.image.pullPolicy }}
        ports:
          - containerPort: 9000
        env:
          - name: PODTATO_PORT
            value: "9000"